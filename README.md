# plain-sight

### Encode **hidden messages** into **png** or **bmp** image pixels

## Requirements

- Linux or MacOS
- `stb` library installed
- `g++` compiler binary available
- `make` installed

## Building

- Run: `make`

## Usage

Encode: `plain-sight [filename] [message]`  
Decode: `plain-sight [filename]`
